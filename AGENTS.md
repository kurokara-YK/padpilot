# AGENTS.md — padpilot

DualSense などのゲームパッドでデスクトップ (マウス・キーボード) を操作する構成を、
`bash install.sh` 一発で入るパッケージとして配布する。

このファイルはリポジトリの**方針**を定める。手順の詳細は README、
実測で得た知見は `docs/` 側に置く。ここに書くのは「なぜそうするか」だけ。

---

## 0. 現状 (2026-09-09 時点)

パッケージ化はまだ始まっていない。今あるのは以下4ファイルだけ。

| ファイル | 中身 | 状態 |
|---|---|---|
| `PS5コントローラー_README.md` | 22KB の詳細手順書。実測ログ付き | **資産。捨てない** |
| `antimicrox-ps5` | 起動ラッパー (bash) | 完成度高い。パス以外は流用可 |
| `antimicrox-ps5.desktop` | GNOME ランチャー定義 | `Exec` が絶対パス直書き |
| `ps5_desktop.gamecontroller_2_2.amgp` | プロファイル (3セット構成) | **ほぼそのまま多機種で使える** (§3) |

既存 README は「試して分かったこと」の記録として質が高い。
バージョン別の実測表、事故の記録 (`pkill -f` で自分を kill した等) は
再取得できない情報なので、パッケージ化の過程で**要約して捨てないこと**。

---

## 1. 大方針

### 1.1 ブランチは分けない。1ブランチで全機種対応

**結論: 機種別ブランチにはしない。**

理由は、機種差がプロファイル XML の中に**ほとんど無い**ため。
AntiMicroX のプロファイルは SDL の GameController 抽象の上に乗っている。
原本の先頭にこう書いてある。

```xml
<!--The SDL name for a joystick is included for informational purposes only.-->
<sdlname>DualSense Wireless Controller</sdlname>
<!--The Unique ID for a joystick is included for informational purposes only.-->
<uniqueID>030057564c050000e60c00000000680013563302</uniqueID>
```

`informational purposes only` = **読み込み時に照合されない**。
SDL が「ボタン1=○/A、ボタン2=×/B」と正規化するので、
PS5 で作ったプロファイルは Xbox コントローラーでもそのまま動く。
物理ボタンの刻印が違うだけで、index は同じ。

ブランチを分けると、共通部分 (ラッパー・.desktop・install.sh) の修正が
毎回 N ブランチへの cherry-pick になる。差分が小さいのに管理コストだけ N 倍。

**採るべき構成: 1 ブランチ + プロファイル差し替え。**

```
profiles/
├── common-desktop.amgp     ← 本体。機種非依存
├── ps5.overrides           ← 差分だけ (touchpad = button 21 等)
└── xbox.overrides          ← 差分だけ (Xbox には touchpad が無い)
```

`install.sh` が接続中のコントローラーを検出して、該当する差分を当てて配置する。

### 1.2 機種差として実在するのは「余剰ボタン」だけ

標準の 1〜11 (A/B/X/Y, L1/R1, back, start, guide, L3/R3) と
スティック・トリガー・十字キーは全機種共通。差が出るのはその外側。

| index | PS5 | PS4 | Xbox | Switch Pro |
|---|---|---|---|---|
| 1〜11 | ○ | ○ | ○ | ○ |
| 21 (touchpad click) | **あり** | あり | **無し** | 無し (キャプチャボタン) |
| ジャイロ/加速度 | あり | あり | 無し | あり |

現行プロファイルで機種依存なのは **button 21 (タッチパッド押し込み → onboard 起動)** のみ。
Xbox ではこの割り当てが単に無視されるだけで、エラーにはならない。
つまり**何もしなくても Xbox で動く**。overrides はそこを明示するための整理であって、
動作のための必須要件ではない。

### 1.3 対応 OS: Linux と Windows の両方。ただし対等ではない

**AntiMicroX 自体は Windows 対応済み** (`antimicrox-X.X.X-AMD64.exe` のインストーラ配布)。
既存 README の「X11 必須」という記述は**AntiMicroX 全体の話ではなく、この Linux 構成の話**。
公式 README は "We support X.org and Wayland" と明記している。
Wayland で落ちるのは **auto-profile (アクティブウィンドウに応じた自動切替) だけ**で、
本構成はその機能を使っていない。

> **要検証**: 本構成が Wayland で実際に動くか。
> 既存 README は「XTest が使えないので Wayland では動作しない」としているが、
> これは 3.6.0 時点の推測を含む可能性がある。実測して README を訂正すること。
> 動くなら「X11 必須」の制約が消え、対応範囲が大きく広がる。

**Windows 対応で問題になるのは AntiMicroX ではなく、周辺すべて。**

| 要素 | Linux | Windows | 移植性 |
|---|---|---|---|
| プロファイル `.amgp` | そのまま | **そのまま使える** | ◎ |
| AntiMicroX 本体 | AppImage | .exe インストーラ | ○ (入手方法が違うだけ) |
| `antimicrox-ps5` ラッパー | bash | **動かない** | ✗ 要 PowerShell 版 |
| `.desktop` ランチャー | GNOME | **概念ごと存在しない** | ✗ 要ショートカット |
| `onboard` (画面キーボード) | `/usr/bin/onboard` | **存在しない** | ✗ 要 `osk.exe` |
| `pgrep` / `xprop` による状態判定 | 前提 | **存在しない** | ✗ 要書き直し |

`.amgp` が両 OS で共通なのが効いている。**資産の中核は移植できる**。
移植が必要なのは「その周りの起動・常駐まわり」だけ。

**段階的にやる。いきなり両対応を目指さない。**

1. **Phase 1** — Linux を綺麗にパッケージ化する (`install.sh`)。今の資産が全部使える
2. **Phase 2** — プロファイルから OS 依存を抜く (§3)。Windows 対応の下地になる
3. **Phase 3** — `install.ps1` を足す。ラッパーは PowerShell で書き直し

Phase 1 を飛ばして両対応から始めると、動く物が無い期間が長くなる。

---

## 2. ライセンス

### 2.1 AntiMicroX は GPL v3。だが本パッケージは GPL にしなくてよい

**核心: バイナリを再配布しなければ GPL の義務は発生しない。**

GPL の義務は「頒布 (distribute) した時点」で生じる。
`install.sh` が実行時に公式 GitHub Releases から `wget` するだけなら、
**こちらは AntiMicroX を一切配布していない**。ユーザーが公式から直接落としているだけ。
したがってソース提供義務も、ライセンス継承義務も生じない。

**「改変禁止」ではない。「配布した瞬間に義務が発生する」。** ここを混同しない。

| 行為 | 可否 | 義務 |
|---|---|---|
| 自分の PC で AppImage を書き換えて使う | 自由 | なし (配布していない) |
| 未改変の公式版を install.sh で取得させる | 自由 | **なし。この構成を採る** |
| 未改変の AppImage をリポジトリに置く | 可 | 対応ソースの提供義務 |
| 改変した AppImage を配布する | 可 | **改変版の全ソース公開義務** |

**やってはいけないこと: リポジトリに AppImage / .exe をコミットする。**
これをやった瞬間に「バイナリの頒布者」になり、対応するソース一式の提供義務
(または書面での提供の申し出) を負う。32MB のバイナリを Git に入れる実利も無い。

どうしても本体の改変が必要になったら、**パッチファイルを MIT で置き、
install.sh が取得後に適用する**。パッチ単体は独立した著作物なので義務が及ばない。

なお**プロファイル (.amgp) の差し替えは「改変」ではない**。
AntiMicroX の正規の入力データであり、バイナリには触れていない。ここは完全に自由。

```sh
# install.sh — この形を守る。同梱しない
wget "https://github.com/AntiMicroX/antimicrox/releases/download/${VER}/AntiMicroX-x86_64.AppImage" \
     -O "$HOME/.local/bin/antimicrox"
```

`.gitignore` に `*.AppImage` と `*.exe` を入れて事故を防ぐ。

### 2.2 自作部分のライセンス: MIT 推奨

自分で書いたもの (ラッパー・install.sh・.desktop・README) は独立した著作物。
GPL ソフトを**呼び出すだけ**では派生著作物にならない (リンクしていない、別プロセス)。

**MIT を推奨。** 理由は、この構成の価値が「設定と手順の知見」にあるため。
誰でも自由に取り込める方が広まる。GPL にすると取り込む側に義務が伝播して敬遠される。

`LICENSE` を MIT で置き、README に以下を明記する。

```markdown
## ライセンス
本リポジトリのスクリプト・設定・ドキュメントは MIT ライセンス。
AntiMicroX 本体は GPL v3 であり、本リポジトリには含まれない。
install.sh が公式リリースから取得する。
```

### 2.3 プロファイル (.amgp) の扱い

自作の設定データなので MIT でよい。AntiMicroX の GUI で生成した XML だが、
中身は「どのボタンに何を割り当てるか」という利用者の選択であって、
AntiMicroX のソースコードを含まない。

---

## 3. タッチパッドの問題

### 3.1 現状: AntiMicroX はタッチパッドを面として扱えない

**これは設定の問題ではなく、AntiMicroX の機能として無い。**

現行プロファイルで使えているのは **button 21 = タッチパッドの「押し込み」だけ**。
物理的なクリックスイッチなので、ただのボタンとして見えている。
指をなぞる **座標 (X/Y)** は AntiMicroX に届いていない。

理由は SDL の層にある。Linux カーネルの `hid-playstation` ドライバは
DualSense を**3つの evdev ノードに分けて**公開する。

```
gamepad      ← スティック・ボタン・トリガー  … SDL が読む。AntiMicroX に届く
touchpad     ← 指の座標 (マルチタッチ)        … 別ノード。AntiMicroX は見ていない
motion       ← ジャイロ・加速度               … 別ノード
```

AntiMicroX は SDL の GameController API しか使わないので、
`touchpad` ノードは管轄外。3.6.1 の変更点も Flatpak 修正と Qt6 移行だけで、
タッチパッド機能は追加されていない (公式 CHANGELOG 確認済み)。
**バージョンを上げても解決しない。**

### 3.2 解決策

実現性の高い順。**推奨は A。**

#### A. カーネルに任せる (推奨・**実測で成立を確認済み**)

**2026-09-09 実測。この方針で確定。**

`hid-playstation` は DualSense を 3 ノードに分けて公開しており、
**touchpad ノードには既に `mouse5` が割り当たっている**。
つまり**カーネルの時点で普通のマウスとして扱われている**。

```
DualSense Wireless Controller                 -> event31  js0      ← gamepad
DualSense Wireless Controller Motion Sensors  -> event256 js1      ← ジャイロ
DualSense Wireless Controller Touchpad        -> mouse5   event257 ← ★ mouse 付き
```

`mouse5` が付いている = X サーバ / libinput がマウスとして読んでいる。
**AntiMicroX は一切関係なく、実装コストはゼロ。**
検証は `check-touchpad.sh` で再現できる。

> 注意: ノート PC 内蔵の `PNP0C50:00 ... Touchpad` と名前が紛らわしい。
> 判定は必ず `DualSense` を含むかで絞ること (`check-touchpad.sh` はそうしている)。

残る確認事項:

- 指でなぞって実際にカーソルが動くか (ノードの存在と実動作は別)
- 左スティックによるカーソル移動と**二重に効かないか** (§8.3 の Steam 競合と同じ構図)
- 感度調整が要るか。要るなら libinput 設定を `install.sh` が置く

**B / C は不要。** 補助デーモンも upstream 待ちも要らなくなった。

> 注意: この場合 AntiMicroX の左スティックによるカーソル移動と**二重に効く**。
> 競合しないか (既存 README §8.3 の Steam 競合と同じ構図) を確認する。

#### B. 補助デーモンを書く (A で不足なら)

`touchpad` evdev ノードを直接読み、`uinput` で仮想マウスとして出す常駐プロセス。
Python + `evdev` ライブラリで 100 行程度。ジェスチャ (2本指スクロール等) も自前で作れる。

- 利点: 挙動を完全に制御できる。AntiMicroX と役割分担が明確
- 欠点: 保守対象が増える。`uinput` に権限設定 (udev ルール) が要る
- **Windows には流用できない**。別実装が必要

A で足りるならやらない。

#### C. upstream に issue / PR を出す

AntiMicroX 側にタッチパッド対応を入れてもらう。
根本解決だが時間軸が読めない。**待たずに A で進める。**
余力があれば並行して issue を立てる (2026-08 時点で該当 issue は未確認)。

#### D. 諦めて別ボタンに寄せる — 非推奨

タッチパッドを使わず、余ったボタンに機能を割り当てる。
既に button 21 で onboard を起動しているので実質これに近い。
「残念」を解消しないので、A を試す前にここへ逃げないこと。

### 3.3 Windows でのタッチパッド

**Windows では A が使えない。** `hid-playstation` は Linux カーネルの機能。
Windows は DualSense を汎用 HID として扱い、タッチパッドを公開しない。
DS4Windows / DualSenseX といった既存ツールが担当する領域なので、
**Windows 版ではタッチパッド対応を目標に入れない**。割り切る。

---

## 4. リポジトリ構成 (目標形)

```
padpilot/
├── AGENTS.md                    ← このファイル
├── README.md                    ← 導入手順。短く保つ
├── LICENSE                      ← MIT
├── install.sh                   ← Linux 用。これ一発
├── uninstall.sh
├── install.ps1                  ← Windows 用 (Phase 3)
├── bin/
│   └── padpilot                  ← 現 antimicrox-ps5。パス直書きを除去
├── profiles/
│   ├── common-desktop.amgp
│   ├── ps5.overrides
│   └── xbox.overrides
├── gui/                         ← PySide6 アプリ (§6)
│   ├── main.py
│   ├── i18n.py                  ← 日本語/英語の辞書
│   └── style.qss
├── share/
│   └── padpilot.desktop  ← Exec は install.sh が書き換える
└── docs/
    ├── versions.md              ← 3.6.1 が使えない理由。実測表
    ├── troubleshooting.md       ← 現 README §8〜9
    └── profile-editing.md       ← 現 README §7.2 の編集規則
```

現 `PS5コントローラー_README.md` は `docs/` へ分割して移す。
情報量を落とさないこと。**要約は劣化**。

### 4.1 install.sh の要件

- **冪等**。何度実行しても壊れない。既存ファイルは退避してから上書き
- **sudo を要求しない**。全部 `$HOME` 内で完結する (現構成の美点。維持する)
- **バージョンを固定**。`VER=3.6.0` を先頭に定数で置く。3.6.1 は使えない (docs/versions.md)
- **前提を検査して落ちる**。`wget` の有無、セッション種別、既存 `antimicro` パッケージ
- **コントローラーを自動検出**してプロファイルを選ぶ。未接続なら PS5 を既定にして警告
- **ダウンロードを検証**。サイズか sha256 を確認する。壊れたファイルを `chmod +x` しない

### 4.3 install / uninstall の設計 (2026-09-09 実装・検証済み)

#### 既定はユーザーの設定そのまま

**`profiles/desktop.amgp` は 2026-08-06 に作った設定の原本そのもの** (md5 `bd328e09...`)。
パッケージ化にあたって内容を変えていない。導入すればいきなり使える状態になる。

| セット | 入り方 | マウス速度 |
|---|---|---|
| 通常 | — | 60 |
| 高速 | R2 長押し | 175 |
| 低速 | L2 長押し | 20 |

カスタマイズはこのファイルを差し替えるか、GUI の「詳細設定」から
AntiMicroX 本体で編集する (§6.2)。

> 残件: プロファイル内に `button 21`(死んでいる、§5.2) と
> `/usr/bin/onboard` の絶対パスが残っている。前者は削除、
> 後者は `@OSK@` プレースホルダ化して install 時に置換すべき (§4.2)。

#### install の段階

1. **preflight** — 前提検査。原本の存在、書き込み先、旧 `antimicro` パッケージ、
   `onboard` の有無、旧構成の残骸。**致命的なものだけ中止**し、
   残りは警告として流す
2. **取得** — 公式リリースから DL。**サイズ検証** (10MB 未満は破損とみなす)。
   壊れたファイルに `chmod +x` しない
3. **配置** — プロファイル、gsettings (画面キーボード)、ランチャー
4. **verify** — 導入後に自動で確認。実行権限、プロファイルの md5 一致、
   ランチャー登録。**入れっぱなしにせず入った結果を確かめる**

#### uninstall の段階

**撤退は導入より難しい。** 消し漏れが次の導入を壊すため。

- **プロセスを名前を問わず止める。** 別名 AppImage は展開先に `antimi` が
  現れず取りこぼす (§6.3 の事故)。撤退時は広く止める
- **旧構成の残骸も消す** (`find_legacy()`)。`~/bin/antimicrox360`、`~/p2.amgp` 等。
  残すと二重起動して「終了させたのに効き続ける」事故になる
- **gsettings を既定へ戻す。** `toolkit-accessibility` はシステム全体の設定で
  padpilot 専有ではない。勝手に変えたものは戻す
- **残留ソケットを必ず消す。** 残すと次回起動できなくなる (§8.1)
- **空ディレクトリを片付ける。** ただし中身が残っていれば消さない
- `--purge` で設定 (`~/.config/padpilot`, `~/.config/antimicrox`) も削除

#### 検証済みの動作 (2026-09-09)

```
uninstall --purge → 痕跡ゼロ (旧構成 5 件 + padpilot 全て削除、gsettings も復帰)
install.sh        → 6.5 秒で完了 (公式から 31MB 実ダウンロード)
verify / doctor   → 問題なし
start             → 常駐、プロファイル読込エラーなし
install 再実行    → 冪等
```

**プロファイルの md5 が導入前後で一致することを確認済み。**
ユーザーの設定が化けていない。

### 4.2 パス直書きの除去

現状 `.desktop` と `.amgp` に `/home/kurokara` と `/usr/bin/onboard` が埋まっている。
**他人の環境で動かない。** `install.sh` がインストール時に置換する。

```sh
sed -e "s|@BINDIR@|$BINDIR|g" -e "s|@OSK@|$OSK|g" \
    share/padpilot.desktop.in > "$APPDIR/padpilot.desktop"
```

画面キーボードも環境で違う (`onboard` / `squeekboard` / Windows は `osk.exe`)。
`install.sh` が検出して埋める。無ければその割り当てだけ空にする。

---

## 5. 作業時の約束

- **README の実測記録を消さない。** バージョン表・事故の記録は再取得できない
- **バイナリをコミットしない。** §2.1。`.gitignore` で機械的に防ぐ
- **「動くはず」で書かない。** 現 README の価値は全部実測に由来する。
  未検証の内容には本ファイルのように「要検証」と明示する
- **Phase を飛ばさない。** Linux が綺麗に入る状態を先に作る
- **プロファイル編集時は既存 README §7.2 の規則に従う。**
  特に trigger の `throttle` は必ず `positivehalf` (DualSense の L2/R2 は静止位置が -32767)
- **`~/p2.amgp` の欠落を疑う。** 症状が「コントローラーを繋いでもアプリが反応しない」でも、
  原因がプロファイル欠落であることがある (§5.1)。切り分けの最初に見る

---

### 5.1 事故記録: プロファイル欠落 (2026-09-09)

**症状**: コントローラーは接続できているのに、アプリが反応しない。以前は動いていた。

**原因**: `~/p2.amgp` が消えていた。原本は無事だったので、配置先だけが失われていた。

**アプリのログにはっきり出ていた**:

```
Profile location /home/kurokara/p2.amgp does not exist.
Closing
```

AntiMicroX は**プロファイルが無いと起動直後に終了する**。
そのためラッパーの `running=no` だけが見え、「コントローラーが繋がらない」ように見える。
**実際にはコントローラーは全く無関係。**

**復旧**: 原本を配置先へコピーするだけ (既存 README §7.3)。

```sh
cp "<原本>/ps5_desktop.gamecontroller_2_2.amgp" ~/p2.amgp
```

**設計への反映**:

1. **`doctor.py` に `profile_missing` を最優先で入れる** (§7.5)。
   自動修復可能 (原本からコピーするだけ) なので「直す」ボタンを付ける
2. **原本と配置先の二重管理そのものが事故の温床。** 既存 README §7.3 が
   「アプリが読むのは原本ではない」と警告しているのは、この構造的な弱さの現れ。
   `install.sh` は**コピーではなくシンボリックリンク**を検討する
   (原本を編集すれば即反映され、消失も起きにくい)。
   ただし GUI からの「保存」がリンクを実体で置き換える可能性があるため、要検証
3. **エラーの切り分け順序を GUI に埋め込む。** 「繋がらない」と感じたときの真因は
   コントローラー側とは限らない。§7.3 の `Status` は
   `controller` と `running` を**別々に**持つこと。これが分かれていれば
   「コントローラーは見えているがアプリが落ちている」と正しく表示できる

**この事故は §6.1 で述べた「GUI が要る理由」の実例**。
ログを `tail` して英語のエラーを読める人でないと原因に辿り着けない。

---

## 5.2 文字入力ができない問題 (2026-09-09 調査)

### 症状

カーソルは動くしクリックもできるが、**Google の検索欄をクリックしても文字が打てない**。
PS5 本体のように画面キーボードが出てほしいが出ない。
プロファイルでは「タッチパッド押し込み → onboard 起動」を割り当てているのに動かない。

### 原因: AntiMicroX にタッチパッドの押し込みが**届いていない**

`jstest` で実測したところ、AntiMicroX (SDL) が見ている gamepad ノードは
**13 ボタンしか持っていない**。

```
Joystick (DualSense Wireless Controller) has 8 axes
and 13 buttons (BtnA, BtnB, BtnX, BtnY, BtnTL, BtnTR, BtnTL2, BtnTR2,
                BtnSelect, BtnStart, BtnMode, BtnThumbL, BtnThumbR)
```

**タッチパッドの押し込みはこの 13 個に入っていない。**
`hid-playstation` はタッチパッドのクリックを **touchpad ノード側の `BTN_LEFT`**
として公開しているため (§3.2 の 3 ノード分割の帰結)。

```
DualSense ... Touchpad -> mouse5 event257
  持つボタン: BTN_LEFT / BTN_TOUCH / BTN_TOOL_FINGER / BTN_TOOL_DOUBLETAP
```

つまり **押し込み = ただの左クリック**として OS へ流れており、
AntiMicroX からは永久に見えない。

**プロファイルの `<button index="21">` は 3 セットすべてで死んでいる。**
これは設定ミスではなく、**構造的に到達不能**。
既存 README §7.1 の「タッチパッド押し込み → onboard 起動」は**実際には動作しない**。
(§3.2 A でタッチパッドがマウスとして動くのと同じ理由で、こちらは動かない。表裏の関係)

### 検証済みの事実

| 項目 | 結果 |
|---|---|
| `onboard` パッケージ | ✅ インストール済み (1.4.1) |
| `onboard` 単体起動 | ✅ 成功。ウィンドウも出る (`WM_CLASS="onboard","Onboard"`) |
| ibus (日本語 IME) | ✅ 稼働中 (`XMODIFIERS=@im=ibus`) |
| onboard auto-show | ✅ **実測で自動表示を確認** (要 `toolkit-accessibility=true`) |
| AT-SPI | ✅ `at-spi-bus-launcher` / `at-spi2-registryd` 稼働中 |
| button 21 が届くか | ❌ **届かない (13 ボタンに含まれない)** |
| button 12/13 (L3/R3) | **プロファイル内で未割り当て** (`<button index="12">` が存在しない) |

> README §7.1 は「L3 押し込み = Shift」「R3 押し込み = 右クリック」と書いているが、
> **プロファイルには button 12/13 の定義が無い**。記述と実体が食い違っている。
> Shift / 右クリックは別のボタンに乗っている可能性が高い。**要再調査。**

### 解決策

#### A. 空いているボタンに onboard を割り当て直す (B の保険として実施)

**button 21 を捨て、AntiMicroX が実際に見える 12/13 (L3/R3) へ移す。**

- 変更は `.amgp` の 3 セット分だけ。コストが最も低い
- L3/R3 は押し込みで、誤爆しにくい
- **ただし README の記述 (L3=Shift, R3=右クリック) との整合を先に確認する**

代替案として「PS ボタン (button 11、現在 Print) を長押し」も使える。
PS5 本体の挙動に近く、直感的。

#### B. auto-show で自動表示させる (**実測で成立を確認。これが本命**)

**2026-09-09 実測。動作を確認済み。**

onboard には `auto-show` 機能があり、**テキスト入力欄にフォーカスが入ると自動で出る**。
zenity のテキスト入力欄を出して実測したところ、**onboard が自動で表示された**。

必要な設定は 2 つ。**両方要る。**

```sh
gsettings set org.gnome.desktop.interface toolkit-accessibility true   # ★ 必須
gsettings set org.onboard.auto-show enabled true
```

**`toolkit-accessibility` が肝。** 既定値は `false` で、この状態では
`auto-show` を true にしても**動かない**。onboard は AT-SPI (支援技術インタフェース)
経由で入力欄を検知するため、AT-SPI が無効だと検知そのものができない。
**ここに気付かないと「設定したのに出ない」で詰まる。**

これが成立すると:

- **ボタン割り当てが不要になる** (§5.2 A の button 21 問題を迂回できる)
- 「Google の検索欄をクリックしたら勝手にキーボードが出る」= **PS5 本体に最も近い挙動**
- コントローラー固有の設定ではないので、**Xbox でも PS4 でも同じように効く**

`install.sh` / GUI がこの 2 つを設定する。**これを既定の方式とする。**

##### 実測時の注意

- **AT-SPI プロセスの確認に `pgrep -x` を使わない。**
  `at-spi2-registryd` は 15 文字を超えるため `pgrep` が
  「pattern that searches for process name longer than 15 characters」と言って
  **必ず 0 件を返す**。`ps -eo comm,pid | grep at-spi` を使う。
  (既存 README §6.2 の `pgrep` の罠とは別種の落とし穴)
- snap 版アプリ (Discord 等) に対しては AppArmor が AT-SPI を拒否する警告が出るが、
  **通常のアプリでは問題なく動く**。この警告は無視してよい

##### 残る検討事項

- 起動済み onboard の**トグル (隠す/出す)**。`onboard` の再実行は二重起動になる
- auto-show が効かないアプリ (snap 版など) の**手動フォールバック**が要る。
  §5.2 A のボタン割り当ては**この保険として残す価値がある**

#### B'. 手動割り当て (auto-show の保険)

auto-show が効かない場面のために、ボタンからも出せるようにしておく。
割り当て先は §5.2 A のとおり **button 12/13 (L3/R3)**。button 21 は使えない。

#### C. 日本語入力

onboard から ibus 経由で日本語が打てるかは**未検証**。
`XMODIFIERS=@im=ibus` は設定されているので通る見込みだが、
onboard は仮想キーボードイベントを送るため IME が反応しない事例がある。**要実測。**

代替として、GUI 側に「よく使う文字列」のスニペット送出機能を持たせる案もある
(URL、メールアドレス等)。onboard にも snippets 機能がある (`gsettings` に `snippets` キーあり)。

#### D. やらないこと

**自前で画面キーボードを実装しない。** onboard で足りる。
IME 連携まで自作すると本体より大きな仕事になる。

---

## 5.3 Bluetooth 接続状態の検出 (2026-09-09 調査)

### 結論: **実装可能。手段も確定済み**

GUI 起動時に「繋がっているか」を判定し、未接続なら案内を出す要件は問題なく作れる。

### 検証済みの取得手段

**方法 1: `/proc/bus/input/devices` を読む (推奨)**

外部コマンド不要、権限不要、**最も速い**。

```
N: Name="DualSense Wireless Controller"
U: Uniq=e8:47:3a:12:72:57                      ← MAC アドレス
S: Sysfs=.../bluetooth/hci0/...                ← ★ "bluetooth" を含む = BT 接続
H: Handlers=event31 js0
```

- **接続の有無** = `DualSense` を含む行があるか
- **接続種別** = `Sysfs` に `bluetooth` が含まれれば BT、なければ USB
- **MAC** = `Uniq` (実測で `bluetoothctl` の表示と一致を確認)

**方法 2: `bluetoothctl devices Connected`**

```
Device E8:47:3A:12:72:57 DualSense Wireless Controller
```

人間可読だが、外部プロセス起動のコストがある。**表示用の補助**に留める。

**方法 3: D-Bus (BlueZ) — ペアリング操作をする場合**

`python3-dbus` と `PyGObject` が**両方とも利用可能**であることを確認済み。
接続状態の監視だけなら方法 1 で足りるが、
**GUI からペアリングを開始する**なら BlueZ の D-Bus API が要る。

### GUI への反映

§7.3 の `Status` に以下を追加する。

```python
controller: str | None      # "DualSense Wireless Controller" / None
transport: str | None       # "bluetooth" / "usb" / None
mac: str | None             # "e8:47:3a:12:72:57"
```

**画面 [2] 接続案内の設計:**

- 未接続なら「PS ボタンと Create ボタンを同時に長押し」を**図で**示す
- ポーリング (§7.3) で検出したら自動で「見つかりました」へ遷移する
- **USB 接続も案内する。** BT が繋がらないときの確実な代替手段
- `bluetoothctl` の `Paired` だが `Connected` でない状態を区別し、
  「ペアリング済み。PS ボタンを押してください」と出し分ける

### 注意点

- **`js0` の番号は固定ではない。** 抜き差しで `js1` 等に変わる。
  番号ではなく**名前 (`DualSense`) で探す**こと
- 判定は `DualSense` だけでなく `Wireless Controller` も見る (PS4 は名前が違う)
- **内蔵タッチパッドと名前が紛らわしい** (§3.2 A の注意と同じ)

---

## 5.3.1 ボタン割り当ての実体 (2026-09-09 確定)

`core/profile.py` で `.amgp` を解析し、**実際の割り当てを確定させた**。
既存 README §7.1 の記述とは**食い違う**。実体が正しい。

| ボタン | 実際の割り当て | README の記述 |
|---|---|---|
| ○ | 左クリック | ○ = 左クリック ✅ |
| × | Enter | × = Enter ✅ |
| □ | Ctrl + C | ✅ |
| △ | Ctrl + V | ✅ |
| L1 | **Esc** | L1 = Backspace ❌ |
| R1 | **Tab** | R1 = Delete ❌ |
| SHARE | **Alt + Tab** | SHARE = Esc ❌ |
| OPTIONS | **Shift** | OPTIONS = Alt+Tab ❌ |
| PS ボタン | **右クリック** | PS = Print ❌ |
| L3 | **Backspace** | L3 = Shift ❌ |
| R3 | **Delete** | R3 = 右クリック ❌ |
| タッチパッド押し込み | onboard 起動 (**届かない**, §5.2) | ✅ (ただし動作しない) |

**§5.2 で「button 12/13 が未割り当て」と書いたのは誤り**だった。
`<button index="12">` という要素が無いだけで、**index 10/11 が L3/R3 に対応**しており
実際には埋まっている。SDL の index と README の物理名の対応がずれていた。

**結論: 空きボタンは無い。** §5.2 A の「L3/R3 へ onboard を移す」案は
既存の割り当てを潰すことになる。**auto-show (§5.2 B) を使う判断が正しかった。**

README (`docs/original-notes.md` §7.1) は**実体に合わせて訂正が必要**。

---

## 5.4 タッチパッド押し込みの結論 (2026-09-09 確定)

### 問い

「AntiMicroX にタッチパッド押し込みが届かない」(§5.2) は結局どうすればいいのか。

### 結論: **標準構成では何もしない。それで困らない。**

押し込みを AntiMicroX に届けるのは**構造的に不可能**だが、
**そもそも届ける必要が無くなった**。理由は 2 つ。

1. **文字入力は auto-show で解決した** (§5.2 B)。
   押し込みに onboard を割り当てる必要そのものが消えた。
   入力欄をクリックすれば勝手に出る方が、ボタンを覚えるより良い
2. **押し込み = 左クリック**として既に機能している。
   タッチパッドをマウスパッドとして使う分には、押し込みがクリックなのは**自然な挙動**

つまり **§5.2 で「死んでいる」と判明した button 21 は、削除してよい。**
代替を用意する必要すらない。

### それでも押し込みを別機能に割り当てたい場合

**技術的には可能。** ただし標準構成には入れない (複雑さに見合わない)。

実測で確認した事実:

| 項目 | 結果 |
|---|---|
| touchpad ノード `/dev/input/event257` | BTN_LEFT を持つ = **イベントは取得できる** |
| 一般ユーザーで開けるか | ❌ **権限不足** |
| gamepad ノード `event31` | ✅ 開ける (`+` = logind の ACL あり) |
| `python3-evdev` | ✅ 導入済み |
| `/dev/uinput` | ✅ `crw-rw-rw-` = **誰でも書ける** |

**なぜ片方だけ開けないのか**: systemd-logind は**ジョイスティックには ACL を付ける**が、
タッチパッドノードには付けない。`getfacl` で確認した。

```
event31  : user:kurokara:rw-   ← ACL あり
event257 : (ACL なし)          ← root:input 0660 のみ
```

**解決するなら udev ルール** (`share/99-padpilot-input.rules` に用意済み)。

```
SUBSYSTEM=="input", ATTRS{name}=="*DualSense*Touchpad*", MODE="0660", GROUP="input"
```

`sudo` と再ログインが要る。**標準構成が sudo 不要である利点を壊す**ため、
「押し込みを別機能に割り当てたい人向けの任意手順」に留める。

その上で evdev で読み、uinput で任意のキーを出すデーモンを書けば実現できる。
**やらない。** §7.8 のスコープ防衛に従う。

---

## 5.5 日本語入力の実測 (2026-09-09)

### 結論: **動く。漢字変換まで確認済み。**

XTest で合成したキー入力が ibus/mozc を正しく通ることを実測した。

```
送出: "ai" → Henkan → "aiueo"
結果: 愛あいうえお
```

**漢字変換 (愛) まで成立している。** onboard も同じ XTest 方式でキーを送るため
(`org.onboard.keyboard key-synth = 'auto'`、XTEST 拡張あり)、
**画面キーボードから日本語が打てる**と判断してよい。

環境:

| | |
|---|---|
| IME | ibus + mozc (`mozc-jp`) |
| 入力ソース | `[('xkb','jp'), ('ibus','mozc-jp')]` |
| `XMODIFIERS` | `@im=ibus` |
| キー送出方式 | XTest (onboard の `key-synth='auto'` が選択) |

### 注意

- **IME のオン/オフ切り替えキーが要る。** 実測では `Henkan` を使った。
  onboard の既定レイアウトに変換キーが無い場合、
  **レイアウトのカスタマイズか snippets での代替**が要る。**要検討**
- `ibus_input_context_post_process_key_event: Type 'h' is not supported` という
  警告が出るが、**入力自体は成功している**。無視してよい

---

## 6. GUI アプリ

### 6.1 なぜ GUI が要るか

`bash install.sh` はプログラマーにしか通じない。**この構成の対象は「PS5 のコントローラーで
PC を操作したい人」であって、ターミナルを開ける人ではない。**
install.sh は GUI の**内部実装**として残すが、利用者の入口にはしない。

さらに、AntiMicroX 本体の GUI は**設定ツール**であって運用ツールではない。
終了ボタンが無い、トレイ前提でウィンドウを閉じると挙動が読めない、
Bluetooth 接続の案内が無い、といった不備がある。
既存の `antimicrox-ps5` ラッパーはこれを bash で埋めていたが、
**その役割ごと GUI へ引き上げる**のが本フェーズ。

### 6.2 GUI が担う範囲

AntiMicroX の設定画面を作り直すのではない。**その手前と外側**を作る。

1. **導入** — AntiMicroX の取得・配置・プロファイル配置 (= 現 install.sh)
2. **接続案内** — 「PS ボタンと Create ボタンを同時に長押し」等を図で示す。
   ペアリング待ちを検出して「見つかりました」まで誘導する
3. **状態表示** — 接続中か / 常駐しているか / どのセットか。**ここが今一番無い**
4. **起動・終了** — 大きなトグル1つ。ラッパーの `start` / `quit` に相当
5. **診断** — 既存 README §9 の調査手順をボタン1つに。X11/Wayland、
   残留ソケット、Steam 競合、バージョン誤りを自動判定して日本語で提示

**設定の細かい編集は AntiMicroX 本体に投げる。**「詳細設定」ボタンで GUI を開くだけ。
プロファイル編集器を自作すると保守対象が本体と二重になる。

### 6.2.1 セットアップウィザード (2026-09-09 実装・検証済み)

**`bash install.sh` はプログラマーにしか通じない** (§6.1)。
Windows のインストーラーと同じ「次へ次へで終わる」体験を用意する。

#### 入口

```
setup.sh → GUI が使えれば gui/installer.py
        → 使えなければ (SSH 等) CLI の install へ自動で落ちる
```

**GUI を必須にしない。** PySide6 が無い環境でも導入できる経路を残す。

#### 6 画面の構成

| # | ページ | 役割 |
|---|---|---|
| 1 | WelcomePage | 何が行われるか。sudo 不要・撤退可能であることを先に示す |
| 2 | CheckPage | **動作環境を自動判定**。コントローラー/セッション/onboard/旧構成 |
| 3 | OptionsPage | アイコン追加・画面キーボード・自動起動・旧構成削除の選択 |
| 4 | ProfilePage | **既定の割り当てを表で見せる** |
| 5 | ProgressPage | 別スレッドで導入。ログを流す |
| 6 | DonePage | 使い方の案内。「今すぐ開始する」 |

#### 設計上の判断

- **CheckPage で止めない。** コントローラー未接続でも導入は進められる。
  導入と接続は別の問題であり、ここで塞ぐと「後で繋ぐ」ができなくなる
- **ProfilePage を挟む理由**: 何がどうなるか分からないまま導入させない。
  `core/profile.py` が `.amgp` を読んで表を生成するので、
  **プロファイルを差し替えれば表示も変わる** (手書きしない)
- **ProgressPage は QThread。** 31MB の DL 中に GUI が固まらないため
- **進捗バーは不定 (range 0,0)。** 段数を偽らない。
  DL 時間は回線次第で、正確な割合は出せない
- **失敗時は理由をそのまま出す。** 握り潰さない。DonePage が失敗を表示し、
  「今すぐ開始する」を無効化する

#### 検証済み (2026-09-09)

全 6 ページの生成と、ウィザードを最後まで進めた実導入を確認。

```
WelcomePage → CheckPage → OptionsPage → ProfilePage → 導入完了 ok=True
  取得中: .../3.6.0/AntiMicroX-x86_64.AppImage
  配置しました: ~/.local/bin/antimicrox (31 MB)
  ...
verify / doctor → 問題なし
プロファイル md5 → 原本と一致
```

#### 7 画面へ改訂 (2026-09-09、利用者検証を受けて)

初回実装の 6 画面には不足があった。実際に触ってもらって判明した点を反映する。

| 指摘 | 対応 |
|---|---|
| 接続を促す画面が無い | **ConnectPage を追加** (2 番目)。接続済みでも「確認しました」を出す |
| 「以前の設定を削除」が押せない | **対象が 0 件なら項目ごと隠す**。グレーアウトで放置しない |
| 割り当てを変更できない | **プルダウンで選べるように** (`core/profile.py` の `CHOICES`) |
| 完了を押すまで動かなかった | **TestPage を追加** (6 番目)。起動してから「確認しました」で完了 |
| 見た目 | Discord 風 (紫 `#5865f2` / 背景 `#313338`) に変更 |

**TestPage が重要。** 導入直後は常駐が始まっておらず、
「完了を押したら動いた」という状態だった。**動いたことを確認してから完了させる。**
ポーリングで状態を見て「✅ 動いています」を出す。

**ConnectPage は接続済みでも飛ばさない。** 「今つながっている」と分かることに
価値がある。未接続なら繋ぎ方を案内し、**繋がった瞬間に自動で画面が変わる**
(1.5 秒ポーリング)。未接続でも「接続しないで先に進む」で続行できる —
導入と接続は別の問題であり、ここで塞ぐと後で繋げなくなる。

#### 割り当ての編集 (`core/profile.py`)

- `CHOICES` に候補を定義し、GUI はそれを出すだけ
- **原本は書き換えない。** 変更は配置先 (`~/.local/share/padpilot/`) にのみ適用
- 変更した場合は `verify(allow_edited=True)` で md5 照合を飛ばす
- タッチパッド押し込み (21) は候補に出さない。届かないため (§5.2)

> 実装中の不具合: `0x20` (スペース) を `chr(32)` で表示すると空白になり、
> 画面上は「割り当てなし」に見えた。表示名を「スペース」に固定して解決。
> **XML は正しく、表示だけの問題だった。**

#### 入口をコマンドから外す (重要)

**対象利用者はターミナルを触ったことがない。**
`bash setup.sh` と打たせる前提は成立しない。

```
padpilot-セットアップ.desktop   ← ダブルクリックで開く
アンインストール.desktop         ← ダブルクリックで削除
```

`.desktop` は `gio set ... metadata::trusted true` で信頼済みにしておく。
GNOME はこれが無いと「信頼できないアプリ」と警告する。

**アンインストールも GUI にした** (`gui/uninstaller.py`)。
消える物を列挙し、確認してから実行する。取り返しがつかない操作のため。

#### 起動方法: GNOME 46 の制約 (2026-09-09 判明)

**`.desktop` をフォルダに置いてダブルクリック、は Ubuntu 24.04 では動かない。**

実測で判明した事実:

| 事象 | 原因 |
|---|---|
| `.sh` をダブルクリック → VS Code が開く | `application/x-shellscript` の既定が `code.desktop` |
| `.desktop` をダブルクリック → 実行されない | **Nautilus 46 は任意の場所の .desktop を実行しない** |
| 右クリック「プログラムとして実行」→ 無反応に見える | 端末が開かないため出力が見えないだけ。**実際は動いている** |
| `gsettings ... executable-text-activation` | **GNOME 46 でキー自体が削除済み** |

`~/.local/share/applications/` に登録したものは `gtk-launch` で起動する
ことを確認済み。**正式な場所に登録されたものだけがアプリとして扱われる。**

**採った方式**: ブートストラップ方式。

```
はじめにこれを実行.sh   ← 右クリック → 「プログラムとして実行する」
   ├─ ~/.local/share/applications/ に .desktop を 2 つ登録
   └─ そのままセットアップを開く
```

**最初の 1 回だけ右クリックが要る。2 回目からはアプリ一覧から起動できる。**
右クリック実行では端末が出ないため、`zenity` で画面に結果を出す
(無反応に見えるのを防ぐ)。

> `.desktop` をリポジトリに置く案は破棄した。GNOME 46 で動かないため。

### 6.2.3 画面キーボードが 1 秒で消える (2026-09-09 修正)

**症状**: 入力欄をクリックすると出るが、すぐ消える。

**原因**: `org.onboard.auto-show hide-on-key-press` の既定が `true`。
これは「物理キーボードを打ち始めたら画面キーボードを引っ込める」機能。

**コントローラーのボタンは AntiMicroX が XTest でキーイベントとして送る**ため
(§5.5 の日本語入力が成立するのと同じ仕組み)、onboard から見ると
「物理キーボードが打たれた」と区別がつかない。結果、出た直後に隠れる。

```sh
gsettings set org.onboard.auto-show hide-on-key-press false
```

**§5.2 B の設定は 2 つでは足りず、3 つ必要だった。**

| 設定 | 値 | 役割 |
|---|---|---|
| `toolkit-accessibility` | true | AT-SPI。これが無いと検知しない |
| `auto-show enabled` | true | 入力欄で自動表示 |
| **`hide-on-key-press`** | **false** | **コントローラー操作で隠れないように** |

`install.py` と `doctor.py` の両方に反映済み。
`doctor` は誤設定を検出して自動修復する。

### 6.2.4 メイン画面 (`gui/main.py`)

セットアップ後の操作は AntiMicroX 本体の GUI に投げていたが、
**分かりにくいという指摘を受けて自前で持つことにした** (§6.2 の方針変更)。

| タブ | 役割 |
|---|---|
| ホーム | 状態表示と開始/停止トグル |
| ボタンの割り当て | **プルダウンで変更 → 保存** |
| 調子を確認する | 旧「診断」。名前を平易にした |

- **「診断」→「調子を確認する」に改名。** 何をするか分からないという指摘に対応
- 保存すると**稼働中なら再起動して即反映**する。プロファイルは起動時にしか
  読まれないため
- **原本は触らない。** 配置先を基点に書き換える
- `.desktop` のアクションも平易化 (「常駐開始」→「コントローラーを使えるようにする」)

> **ライセンス上の確認**: AntiMicroX 本体の GUI は**改造していない**。
> `.amgp` は AntiMicroX の正規の入力データであり、それを書くのは改造ではない。
> padpilot は別プロセスの独立したアプリなので、GPL の義務は生じない (§2.1)。

#### 見つけた不具合 (追加)

| 不具合 | 原因 | 対処 |
|---|---|---|
| ウィンドウを閉じると落ちる | `QThread` 実行中に破棄。`QThread: Destroyed while thread is still running` | `closeEvent` で `wait()` してから閉じる |
| 点検の二重起動 | ボタン連打で `QThread` が重複 | `isRunning()` で弾く |

### 6.2.3 画面キーボードが 1 秒で消える (2026-09-09 修正)

**症状**: 入力欄をクリックすると出るが、すぐ消える。

**原因**: `org.onboard.auto-show hide-on-key-press` の既定が `true`。
「物理キーボードを打ち始めたら画面キーボードを引っ込める」機能。

**コントローラーのボタンは AntiMicroX が XTest でキーイベントとして送る**ため
(§5.5 の日本語入力が成立するのと同じ仕組み)、onboard からは
「物理キーボードが打たれた」と区別がつかない。結果、出た直後に隠れる。

**§5.2 B の設定は 2 つでは足りず、3 つ必要だった。**

| 設定 | 値 | 役割 |
|---|---|---|
| `toolkit-accessibility` | true | AT-SPI。これが無いと検知しない |
| `auto-show enabled` | true | 入力欄で自動表示 |
| **`hide-on-key-press`** | **false** | **コントローラー操作で隠れないように** |

`install.py` と `doctor.py` の両方に反映済み。`doctor` が誤設定を自動修復する。

### 6.2.4 メイン画面 (`gui/main.py`) — 2026-09-09 追加

セットアップ後の操作は AntiMicroX 本体の GUI に投げる方針だったが、
**分かりにくいという指摘を受けて自前で持つことにした** (§6.2 の方針変更)。

| タブ | 役割 |
|---|---|
| ホーム | 状態表示と開始/停止トグル |
| ボタンの割り当て | **プルダウンで変更 → 保存** |
| 調子を確認する | 旧「診断」。名前を平易にした |

- **「診断」→「調子を確認する」に改名。** 何をするか分からないという指摘に対応
- 保存すると**稼働中なら再起動して即反映**。プロファイルは起動時にしか読まれない
- **原本は触らない。** 配置先を基点に書き換える
- `.desktop` のアクションも平易化 (「常駐開始」→「コントローラーを使えるようにする」)

> **ライセンス上の確認**: AntiMicroX 本体の GUI は**改造していない**。
> `.amgp` は AntiMicroX の正規の入力データであり、それを書くのは改造ではない。
> padpilot は別プロセスの独立したアプリなので GPL の義務は生じない (§2.1)。
> 本体を改変して配布する場合のみ、改変版の公開義務が生じる。

#### 見つけた不具合 (追加)

| 不具合 | 原因 | 対処 |
|---|---|---|
| ウィンドウを閉じると落ちる | `QThread` 実行中に破棄 (`Destroyed while thread is still running`) | `closeEvent` で `wait()` |
| 点検の二重起動 | ボタン連打で `QThread` 重複 | `isRunning()` で弾く |

### 6.2.5 画面キーボードが消える — 真因は 2 つ目にあった (2026-09-09)

`hide-on-key-press` だけでは直らなかった。**onboard の debug ログで確定**。

```
$ onboard -d info
AutoShow  can_show_keyboard: locks=[] tablet_mode=None      ← 出せない
```

**`tablet-mode-detection-enabled` が true だと `tablet_mode=None` になる。**
判定に acpid が要るが、この環境には無い (`Failed to connect to acpid`)。
onboard は「タブレットかどうか分からない」状態では出してよいか判断できず、
数秒で引っ込む。切ると `tablet_mode` の行が消え、表示され続ける。

**必要な設定は 4 つ。** §5.2 B / §6.2.3 の記述を更新する。

| 設定 | 値 |
|---|---|
| `toolkit-accessibility` | true |
| `auto-show enabled` | true |
| `hide-on-key-press` | false |
| **`tablet-mode-detection-enabled`** | **false** |

> **教訓**: gsettings を総当たりで推測せず、**アプリ自身の debug ログを見る**。
> `onboard -d info` が理由を直接教えてくれた。

### 6.2.6 割り当ての全面拡張 (2026-09-09)

「割り当てが一部しかない」「Fキーが無い」「3つ以上の組み合わせができない」
という指摘に対応。**選択肢を 21 → 134 に拡張。**

| 種類 | 内容 |
|---|---|
| 文字 | A-Z, 0-9, 記号 |
| ファンクション | **F1〜F35** |
| 特殊キー | Enter/Esc/Tab/矢印/Home/PageUp/半角全角/変換 など |
| マウス | 左右中クリック、**ホイール上下左右**、戻る・進む |
| 組み合わせ | プリセット 15 種 + **任意個の自作** |

**設計**: `CHOICES` のタプル列を捨て、`KEYS` (表示名→キーコード) の辞書に変更。
組み合わせは `"Ctrl + Shift + F5"` という表示名を `_parse_combo` で分解する。
**表示名がそのまま識別子**なので、往復が保証しやすい (134/134 で検証)。

#### セット対応

AntiMicroX の「セット」(1 プロファイルに複数の割り当てを持つ機能) を GUI に出した。

- `bindings_for_set` / `apply_set_bindings` — **セットごとに編集**
- `get_speeds` / `set_speeds_for` — **カーソル・スクロール速度を変更**
- 既定の 3 セット (通常/R2 高速/L2 低速) をそのまま見せる

#### ホイールでコンボが変わる不具合

`QComboBox` は既定でホイールに反応する。**表の中に置くと、
スクロールしたつもりが値を書き換えてしまう。**
`gui/widgets.py` の `NoScrollComboBox` で、閉じているときはホイールを
親へ流すようにした。

### 6.2.7 ダウンロード失敗 (HTTP 500) — 2026-09-09

**症状**: セットアップ中に `HTTP Error 500: Internal Server Error` で失敗。

**原因**: **GitHub の配信側の一時的な障害。padpilot 側のバグではない。**

切り分けの記録:

| 検証 | 結果 |
|---|---|
| `curl -I` で同じ URL | ✅ HTTP 200、32MB |
| `urllib.urlopen` (再現時) | ❌ HTTP 500 |
| User-Agent を変えて 4 種 | **全部 ✅ 200** |
| 同じ URL を 12 回連続 | **12/12 ✅ 200** |

User-Agent 依存を疑ったが**否定された**。同一条件で成功したり失敗したりする
= サーバ側の断続的な障害。

> **危うかった判断**: 最初 `Python-urllib/3.12` という UA が弾かれたと考えた。
> しかし追試で全 UA が成功したため**その仮説は捨てた**。
> 1 回の観測で原因を決めない。

**対処**: バグではないが、**1 回の失敗でセットアップ全体が止まるのは欠陥**。

1. **最大 4 回の再試行** (3 秒 → 6 秒 → 9 秒と待つ)
2. **エラーを日本語に翻訳** (`_describe_error`)。
   「HTTP 500」ではなく「配布元が応答しません」と出す
3. **失敗時に「次へ」を無効化。** 進んでも壊れた状態になるだけ
4. **「もう一度試す」ボタン**を出す

実測で 500 が 2 回起きても 3 回目で成功することを確認済み。

> **UA も設定した** (`padpilot/0.1`)。原因ではなかったが、
> 素性を名乗るのは礼儀であり、将来 UA でブロックされる事態も避けられる。

### 6.2.8 画面キーボードが消える — 真因は複数モニタ (2026-09-09 解決)

**3 度目の調査でようやく特定。** 前 2 回の修正 (§6.2.3, §6.2.5) は
どちらも必要だったが、**本命はこれだった。**

#### 利用者の仮説

「タッチパッドに指が触れると閉じる？」「カーソルを少しでも動かすと閉じる？」

**後者がほぼ正しかった。正確には「カーソルが別のモニタへ移ると消える」。**

#### 実測

`docking-monitor='active'` (既定) で、カーソルを動かして追跡した。

```
初期状態          : 表示 @(0,1235) HDMI-1-0
同じ画面内で移動  : 表示のまま          ← 消えない
別モニタへ移動    : 表示 → 1.5秒後 消滅  ← ★
元の画面へ戻す    : 消えたまま復帰しない  ← ★★
```

**同一モニタ内では消えない。モニタをまたいだ瞬間に消える。**
しかも戻しても復帰しない。これが「すぐ消える」の正体。

この環境は 3 画面 (HDMI 3440x1440 / DP 2560x1440 / ノート 1920x1080)。
`docking-monitor='active'` は「アクティブな画面」に追従する設定で、
カーソル移動でアクティブ画面が変わると、onboard はウィンドウを
作り直そうとして消える。

#### 修正

```sh
gsettings set org.onboard.window docking-monitor primary
```

修正後、同じテストで **32 サンプル全て「表示」** (修正前は 2.8 秒で消滅)。

**必要な設定は 5 つになった。**

| 設定 | 値 | 理由 |
|---|---|---|
| `toolkit-accessibility` | true | AT-SPI。無いと検知しない |
| `auto-show enabled` | true | 入力欄で自動表示 |
| `hide-on-key-press` | false | コントローラー操作で隠れない |
| `tablet-mode-detection-enabled` | false | acpid 不在で判定不能になる |
| **`window docking-monitor`** | **primary** | **モニタ移動で消えない** |

#### GUI に「画面キーボード」タブを追加

`primary` はこの環境ではノート画面。ウルトラワイドで作業していると
キーボードだけノート側に出て不便。**どの画面に出すかを選べるようにした**
(`core/display.py` が `xrandr` からモニタ一覧を作る)。

「動いている画面」を選ぶと消えることも画面上で警告する。
閉じ方 (入力欄以外をクリック / 右上の ×) も明記した。

> **教訓**: 「すぐ消える」という同じ症状に、**独立した原因が 3 つ**あった。
> 1 つ直して直らないとき、前の修正が無駄だったとは限らない。
> 症状ではなく**条件を変えて切り分ける** (同一モニタ内 vs モニタ間) のが効いた。

### 6.2.9 onboard はカーソルを重ねると **クラッシュする** (2026-09-09)

**これまでの「消える」の最終的な真因。設定では直らない。**

#### 症状

キーボードにマウスカーソルを重ねた瞬間、キーボードが消える。戻らない。

#### 実測

```
$ /usr/bin/onboard -d debug > log 2>&1 &
（カーソルをキーボードに重ねる）
Segmentation fault (コアダンプ)     ← 終了コード 139 = SIGSEGV
```

**3 回中 3 回とも SIGSEGV。100% 再現。**
`/var/crash/_usr_bin_onboard.1000.crash` に `Signal: 11` として記録されている。

#### 切り分け

| 条件 | 結果 |
|---|---|
| 入力欄と同じモニタ上で重ねる | ❌ クラッシュ (モニタ跨ぎではない) |
| **auto-show を無効にして重ねる** | ❌ **クラッシュ** |
| `touch-input` を single / none | ❌ クラッシュ |
| `force-to-top=false` | ❌ クラッシュ |

**auto-show を切ってもクラッシュする** = 表示制御の問題ではなく、
**onboard 本体がポインタ進入イベントで落ちている。**

#### 意味すること

**onboard ではキーを押せない。** 押すにはカーソルを重ねる必要があるが、
重ねた瞬間に落ちるため、**この構成の「コントローラーで文字を打つ」は
onboard では成立しない。**

§5.2 B で「auto-show で解決」としたのは**不十分だった**。
表示までは成立したが、**使えなかった。**

#### 対処: onboard を諦める

`onboard 1.4.1-5ubuntu6` (2024 年のビルド) の不具合。
GPL のアプリなので padpilot 側から修正できない (§2.1)。

| 候補 | 評価 |
|---|---|
| **GNOME 内蔵 OSK** | **本命。** `gnome-shell` が描画するので別プロセスが落ちる構造自体が無い。追加インストール不要 |
| `xvkbd` | 入手可 (4.1)。古いが単純で堅牢 |
| `matchbox-keyboard` | 入手可。組み込み向けで軽い |
| `onboard` | **不採用。ホバーで落ちる** |
| `florence` / `squeekboard` | Ubuntu 24.04 のリポジトリに無い |

**次の作業**: GNOME 内蔵 OSK (`org.gnome.desktop.a11y.applications
screen-keyboard-enabled`) が X11 セッションで実用になるか実測する。
ならなければ `xvkbd` に切り替える。

> **教訓**: 「消える」を 4 回追いかけ、うち 3 回は設定の問題として直した。
> しかし最後は**アプリ自体のクラッシュ**だった。
> `-d debug` でログを取ったとき Segmentation fault が出ていたのに、
> ログの中身ばかり見て**終了コードを見ていなかった**。
> プロセスが死んでいないか、まず確認する。

### 6.2.2 リファクタリングとデバッグ (2026-09-09)

#### 静的解析

`pyflakes` で未使用 import を 5 件除去。警告ゼロ。

#### 見つけた不具合

| # | 不具合 | 原因 | 対処 |
|---|---|---|---|
| 1 | 「スペース」が空欄に見える | `0x20` を `chr(32)` で表示 | 表示名を固定 |
| 2 | 画面キーボード割り当てが往復しない | `execute` の表示が `起動: onboard` になり CHOICES と不一致。**再度開くと候補が重複** | 表示名を `画面キーボードを出す` に正規化 |
| 3 | `verify` が編集済みを誤警告 | 原本との md5 照合が、意図的な変更も「異常」と判定 | `.customized` マーカーで区別 |
| 4 | `changes()` が導入後に空になる | 現在値を毎回ディスクから読むため、導入後は「変更なし」と判定 | 初期値を `initial` に保持 |

**2 と 4 は GUI を再度開かないと露見しない。** 往復テストで検出した。

#### 回帰テスト

```
全21候補の往復一致    ✅ 21/21
原本が無傷            ✅
異常系 (欠損/壊れXML) ✅ 落ちない
i18n 日本語/英語      ✅
core が Qt 非依存      ✅
verify / doctor       ✅
```

#### 残件

- **アイコンが未設定** (`input-gaming` を借りている)。専用アイコンを作る
- OptionsPage の選択を config.json に保存していない。再セットアップ時に既定へ戻る
- Windows 版は未着手 (§6.8)

### 6.3 フレームワーク: PySide6 を採る

**Qt が嫌いというのは分かるが、ここでは PySide6 を推す。**
ただし「Qt の流儀で書く」必要は無い (§6.4)。

検討した候補と却下理由。

| 候補 | 評価 |
|---|---|
| **PySide6** | **採用。** 唯一 Linux/Windows でネイティブに動き、配布実績が厚い |
| Flet | Apache-2.0 で**ライセンスは最も楽**。ただし Flutter エンジン同梱で 100MB+、Windows ビルドに Visual Studio C++ が要る。入力ユーティリティには重すぎる |
| NiceGUI | 実体はブラウザ。ローカルに Web サーバを立てる構成になり、「コントローラーを繋ぐデスクトップ常駐アプリ」と噛み合わない |
| Tkinter | 軽いが見た目が古い。**一般利用者に配るアプリ**の入口としては弱い |
| Tauri | Rust + Web。Python 資産と繋がらず、この規模には過剰 |

決め手は**依存の少なさと配布のしやすさ**。既に AntiMicroX 自体が Qt アプリなので、
Linux では Qt ランタイムが実質そこにある。二重に別の GUI エンジンを持ち込む理由が無い。

### 6.4 「Qt が嫌い」への対処

嫌われる原因はたいてい **Qt Designer / .ui ファイル / シグナル・スロットの作法**であって、
PySide6 そのものではない。以下を守れば Qt らしさはほぼ出ない。

- **`.ui` ファイルと Qt Designer を使わない。** レイアウトは Python コードで直に書く
- **`.qss` (スタイルシート) で見た目を作る。** CSS とほぼ同じ書き方
- **シグナル接続は `button.clicked.connect(self.on_click)` だけ。** これ以上の Qt 機構を使わない
- **状態は素の Python オブジェクトで持つ。** `QAbstractItemModel` 等には触らない

画面数も 3〜4 枚程度。Qt の深いところに入る必要が無い規模。

### 6.5 ライセンス上の注意 (重要)

**PySide6 は LGPL v3。** MIT の自作コードから使うこと自体は問題ない。
ただし**配布形態を誤ると義務が発生する。**

- ❌ **PyInstaller の `--onefile` を使わない。**
  LGPL は「利用者が LGPL 部分を差し替えられること」を要求する。
  onefile は Qt の共有ライブラリを単一実行ファイルに埋め込むため、差し替えが事実上不可能になる
- ✅ **`--onedir` を使う。** Qt の `.so` / `.dll` がディレクトリ内に個別に置かれ、
  利用者が差し替えられる = LGPL の要求を満たす
- ✅ LGPL の全文を同梱し、README に「PySide6 は LGPL v3」と明記する

**Linux では PyInstaller すら使わない案もある。** `install.sh` が
`pip install --user PySide6` するだけなら、PySide6 は完全に別パッケージのままで、
LGPL の論点自体が消える。**AntiMicroX と同じ「同梱せず取得する」方針で一貫する。**
PyInstaller が要るのは Python を前提にできない Windows 側だけ。

### 6.6 多言語対応 (日本語デフォルト + 英語)

**Qt の翻訳機構 (`.ts` / `linguist`) は使わない。** §6.4 の方針に反する。

素の Python の辞書で足りる規模。

```python
# i18n.py
LANGS = {
    "ja": {"connect": "コントローラーを接続", "quit": "終了"},
    "en": {"connect": "Connect controller", "quit": "Quit"},
}
def t(key): return LANGS[current_lang].get(key, LANGS["ja"][key])
```

- **既定は日本語。** `locale` を見て `ja` 以外なら英語に落とす
- 画面右上に言語切替を置き、選択を設定ファイルに保存する
- **文字列をコード中に直書きしない。** 最初から `t("...")` を通す。後から剥がすのは高コスト

### 6.7 GUI と install.sh の関係

**install.sh を GUI で置き換えるのではなく、GUI から呼ぶ。**

```
GUI (PySide6)
 ├─ 「インストール」ボタン → install.sh を subprocess で実行し、出力を画面に流す
 ├─ 「起動 / 終了」トグル   → 現 antimicrox-ps5 ラッパー相当
 └─ 「診断」ボタン          → 検査コマンド群を実行して結果を日本語化
```

こうすると **CLI 派は install.sh を直に叩け、一般利用者は GUI を使う**。
両立するうえ、install.sh は GUI 抜きで単体テストできる。
**GUI の中にインストール処理を書き込まないこと。** 再現とデバッグが困難になる。

### 6.8 Windows での配布

- GUI は PySide6 のまま動く (Windows 対応済み)
- `install.ps1` 相当の処理は **GUI 内の Python で実装**して差を吸収する
- 配布は PyInstaller `--onedir` + インストーラ (Inno Setup 等)。**onefile 不可** (§6.5)
- タッチパッド対応は Windows では目標に入れない (§3.3)

---

## 7. 設計 (実装に入る前に決めておくこと)

§1〜6 は「何をするか / なぜそうするか」。ここは「**どう作るか**」を決める。

### 7.1 全体構造: 3層に分ける

**最重要の設計判断。GUI・CLI・ロジックを混ぜない。**

```
┌─────────────────┐  ┌──────────────┐
│  gui/ (PySide6) │  │ install.sh   │   ← 入口は2つ
│  一般利用者向け  │  │ CLI 派向け    │
└────────┬────────┘  └──────┬───────┘
         │                  │
         └────────┬─────────┘
                  ▼
        ┌──────────────────┐
        │  core/ (Python)  │   ← 実処理はここだけ。GUI に依存しない
        │  検出/導入/起動/診断 │
        └──────────────────┘
```

**`core/` は PySide6 を import しない。** これを守ると:

- `python3 -m core.doctor` のように**単体で動かして検証できる**
- GUI を Flet 等に差し替えても core は無傷 (§6.3 の保険)
- 自動テストが書ける (GUI はテストが難しい)

**GUI は「core を呼んで結果を表示する」だけ**にする。
GUI のイベントハンドラの中に判定ロジックを書き始めたら設計が壊れている合図。

> §6.7 で「GUI が install.sh を subprocess で呼ぶ」と書いたが、**これを改める**。
> Windows に bash が無いため、install.sh 依存だと Windows で GUI が動かない。
> **正しくは「GUI も install.sh も core を呼ぶ」。** install.sh は Linux 利用者向けの
> 薄い入口 (`python3 -m core.install` を叩くだけ) に留める。

### 7.2 core のモジュール構成

| モジュール | 責務 | OS 依存 |
|---|---|---|
| `core/detect.py` | コントローラー検出、機種判定、セッション種別 (X11/Wayland) | **あり** |
| `core/install.py` | AntiMicroX 取得・配置、プロファイル配置、ランチャー登録 | **あり** |
| `core/runner.py` | 起動・終了・状態取得 (現 `antimicrox-ps5` ラッパー相当) | **あり** |
| `core/doctor.py` | 診断。既存 README §9 の検査群 | **あり** |
| `core/profile.py` | `.amgp` の読み書き、overrides 適用、パス置換 | なし |
| `core/paths.py` | OS ごとの配置先を一元管理 | **あり** |

**OS 依存を `paths.py` と各モジュール内の分岐に閉じ込める。**
`if platform.system() == "Windows"` を GUI 側に書かない。

### 7.3 状態モデル (GUI の中心)

GUI が表示すべき状態を最初に固定する。ここが曖昧だと画面が破綻する。

```python
@dataclass
class Status:
    installed: bool          # AntiMicroX が配置済みか
    version: str | None      # 3.6.0 期待。違えば警告 (docs/versions.md)
    controller: str | None   # "DualSense" 等。None = 未接続
    running: bool            # 常駐しているか
    has_window: bool         # GUI ウィンドウが在るか
    session: str             # "x11" / "wayland" / "windows"
    problems: list[Problem]  # doctor の検出結果
```

**`Status` を 1〜2 秒ごとにポーリングして画面へ反映する。**
イベント駆動にしない。コントローラーの抜き差しやプロセスの異常終了は
通知が来ないので、**定期的に見に行くのが唯一確実**。

現ラッパーが `pgrep -x` + `/proc/PID/cmdline` で判定している方式 (既存 README §6.1〜6.3)
を `runner.py` にそのまま移植する。**`pgrep -f` を使わない理由も含めて移す** (事故の記録)。

### 7.4 画面構成 (4枚)

```
[1] ようこそ / 導入        … 未導入のとき最初に出る。install を実行
[2] 接続案内               … ペアリング手順を図で。検出まで誘導
[3] ホーム (常時これ)      … 状態表示 + 起動/終了トグル + 「詳細設定」
[4] 診断                   … doctor の結果を日本語で。各項目に「直す」ボタン
```

**起動時に `Status` を見て行き先を決める**:
未導入 → [1] / 導入済みだが未接続 → [2] / それ以外 → [3]

「詳細設定」は AntiMicroX 本体の GUI を開くだけ (§6.2)。プロファイル編集器は作らない。

### 7.5 エラーの扱い

**この構成は「失敗の種類が既に分かっている」のが強み** (既存 README §8)。
未知の例外を出さず、既知の問題として日本語で提示する。

```python
@dataclass
class Problem:
    id: str          # "stale_socket" / "wrong_version" / "wayland" / "steam_conflict"
    severity: str    # "error" / "warning"
    message: str     # 日本語の説明 (t() 経由)
    fix: Callable | None   # 自動修復できるなら関数。無ければ None
```

既存 README §8 の各項目をそのまま `Problem` に落とす:

| id | 対応する既存 README | 自動修復 |
|---|---|---|
| `profile_missing` | §5.1 プロファイル欠落 | ✅ 原本からコピー |
| `no_controller` | §5.3 未接続 | ❌ 接続案内を表示 |
| `osk_unreachable` | §5.2 button 21 が届かない | ✅ 割り当てを 12/13 へ移す |
| `a11y_disabled` | §5.2 B `toolkit-accessibility=false` | ✅ gsettings で有効化 |
| `stale_socket` | §8.1 残留ソケット | ✅ `rm` するだけ |
| `wrong_version` | §3 3.6.1 / 3.1.4 | ✅ 3.6.0 を入れ直す |
| `ghost_process` | §8.2 別インスタンス残留 | ✅ kill |
| `steam_conflict` | §8.3 Steam 競合 | ❌ 案内のみ |
| `wayland` | §1.3 (要検証) | ❌ 案内のみ |

**「直す」ボタンで潰せるものは潰す。** これが GUI 化の最大の価値。
現状これらは全部「README を読んでコマンドを打つ」必要がある。

### 7.6 設定ファイル

```
~/.config/padpilot/config.json    # Linux
%APPDATA%\gamepad-desktop\config.json    # Windows
```

保存するのは `lang` ("ja"/"en")、`profile` (使用中のプロファイル名)、
`autostart` (bool) 程度。**最小限に留める。**
AntiMicroX 本体の設定は本体に任せ、こちらで二重管理しない。

### 7.7 実装順序

**動くものを常に保ちながら進める。** 各段階で単体で価値がある。

1. `core/detect.py` + `core/doctor.py` — **CLI で動く診断ツール**として完成させる。
   これだけでも既存 README §9 の手作業が消える。§8 のタッチパッド検証にも使う
2. `core/install.py` + `install.sh` — 導入を自動化。ここまでで CLI 派には十分
3. `core/runner.py` — 現ラッパーの移植。bash 版と挙動を突き合わせて確認
4. `gui/` — 画面 [3] ホームだけ先に作る。core が揃っていれば薄く済む
5. 画面 [1][2][4] を足す
6. Windows 対応 — `paths.py` と各モジュールの分岐を埋める

**1 を最初にやる理由**: 検出ロジックは他の全モジュールの土台であり、
かつ**§8 のタッチパッド確認そのものに使える**。無駄がない。

### 7.8 やらないこと (スコープ防衛)

- **プロファイル編集 GUI** — 本体がやる。二重実装は保守が破綻する
- **AntiMicroX 本体の改造** — §2.1 の GPL 論点に踏み込む。避ける
- **ジャイロ対応** — SDL 経由では取れない。タッチパッド (§3) と同じ壁。今回は対象外
- **ゲーム用プロファイル** — 本構成は「デスクトップ操作」に限定する
- **自動アップデータ** — バージョン固定 (3.6.0) が前提。自動更新は 3.6.1 を掴んで壊れる

---

## 8. 次にやること

1. **タッチパッドが既に OS から見えているか確認** (§3.2 A)。コントローラーを繋いで実行。
   ここが最大の未確定点で、結果次第で §3 の方針が変わる
2. **Wayland で動くか実測** (§1.3)。動けば「X11 必須」の制約が消える
3. `git init` + MIT LICENSE + `.gitignore` (`*.AppImage`, `*.exe`)
4. `core/detect.py` + `core/doctor.py` (§7.7 の 1)。**ここから実装を始める**
5. `core/install.py` + `install.sh` (§4.1 の要件、§4.2 のパス除去)
6. `core/runner.py` — 現 bash ラッパーの移植
7. `gui/` — ホーム画面から (§7.4)。最初から `t()` を通す (§6.6)
8. 現 README を `docs/` へ分割
9. Xbox など別機種でプロファイル無改造検証 (§1.1 の裏付け)

**GUI (7) を先にやらないこと。** core が無いと GUI は空の箱にしかならず、
検証もできない (§7.1)。
